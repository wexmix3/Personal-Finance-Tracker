# =============================================================
# Backend API
# =============================================================
# Base URL for the FastAPI backend (no trailing slash)
NEXT_PUBLIC_API_URL=http://localhost:8000

# =============================================================
# Plaid
# =============================================================
# Public key shown in Plaid Dashboard → Keys (safe to expose)
NEXT_PUBLIC_PLAID_ENV=sandbox
